from sklearn importdatasets; from sklearn.preprocessing importStandardScaler; data = StandardScaler().fit_transform(datasets.load_iris().data); scipy.optimize.minimize(lambda x: np.sum(np.abs(x-data)), [0,0,0,0], method='BFGS')